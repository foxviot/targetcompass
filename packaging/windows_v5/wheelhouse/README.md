Place offline Python wheels here for air-gapped installation.

Minimum expected dependency:

- python-docx and its transitive dependencies

If this folder contains wheels, the installer uses `pip install --no-index --find-links`.
